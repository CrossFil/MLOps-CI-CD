replicaCount: 1

image:
  repository: crossfil/aiops-service
  pullPolicy: IfNotPresent
  tag: "v1"

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "8000"
