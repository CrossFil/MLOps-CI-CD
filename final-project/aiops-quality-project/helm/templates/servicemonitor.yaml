apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aiops-service-monitor
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app: aiops-service
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
